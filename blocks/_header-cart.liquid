{% doc %}
  Header cart icon. Renders as a button that opens the cart drawer via
  `src/scripts/header-cart.ts` (`[data-header-cart]`). It is deliberately NOT a
  `/cart` link — the cart page is only reached by navigating to `/cart`
  directly.

  @example
  {% content_for 'block', type: '_header-cart', id: 'header-cart' %}
{% enddoc %}

{% liquid
  assign cart_label = 'accessibility.cart' | t
%}

<div class="relative inline-flex">
  {% render 'button',
    icon: 'cart',
    icon_size: 24,
    variant: 'icon',
    aria_label: cart_label,
    attrs: 'data-header-cart'
  %}
  <span
    data-cart-badge
    aria-hidden="true"
    class="bg-brand pointer-events-none absolute -top-0.5 -right-0.5 size-2 rounded-full animate-pulse {% if cart.item_count == 0 %}hidden{% endif %}"
  ></span>
</div>

<script src="{{ 'header-cart.js' | asset_url }}" type="module" fetchpriority="low"></script>

{% schema %}
{
  "name": "t:names.cart",
  "tag": null
}
{% endschema %}
