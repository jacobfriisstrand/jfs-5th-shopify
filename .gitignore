# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
node_modules/

# Shopify CLI files
.shopify/

## Release files
release
*.zip

# Build outputs
dist/

# Compiled / generated theme assets — `assets/` is build output (esbuild,
# Tailwind, copied static files). Source of truth lives in `src/`. Run
# `npm run build` to regenerate.
assets/

# Local development files
.env.*

# NOTE: sections/header-group.json + sections/footer-group.json are NOT ignored
# anymore — they are committed because the production deploy runs without
# `--nodelete` and would delete them from the live theme otherwise.
