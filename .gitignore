# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
node_modules/

# Shopify CLI files
.shopify/

## Release files
release
*.zip

# Build outputs
dist/

# Compiled / generated theme assets — `assets/` is build output (esbuild,
# Tailwind, copied static files). Source of truth lives in `src/`. Run
# `npm run build` to regenerate.
assets/

# Local development files
.env.*
